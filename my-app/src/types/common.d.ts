export type AddressType = {
  id: number;
  country: string;
  state: string;
  city: string;
  street: string;
};
export type ApiParamsType = {
  limit: number;
  page: number;
};

export type PageLimitType = {
  page: number;
  limit: numbeer;
};
